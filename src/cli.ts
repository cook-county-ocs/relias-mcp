#!/usr/bin/env node
/**
 * CLI entry point. Built out in F5 (P5) with commander:
 *   snapshot | reconcile <file> | diff | doctor
 *
 * P0 stub: prints identity and exits 0 so `npm run dev:cli` is wired.
 */
import { PACKAGE_NAME } from './lib/index.js';

console.log(`${PACKAGE_NAME} cli — not yet implemented (F5)`);
